export const LoggedInHeader = () => {
  return (
    <header className={`flex items-center justify-between bg-slate-500 w-full`}>
      <h1 className={`text-2xl`}>LoggedInHeader</h1>
    </header>
  );
};
